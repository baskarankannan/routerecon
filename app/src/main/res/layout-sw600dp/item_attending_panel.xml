<?xml version="1.0" encoding="utf-8"?>


<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="@dimen/field_padding_margin"
        android:text="@string/attending_trips"
        android:textColor="@color/blue"
        android:textSize="@dimen/login_sign_up_text_size"
        android:textStyle="bold" />

    <include layout="@layout/item_attending_shimmer_layout" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_attending_trips"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/field_padding_margin" />

    
</LinearLayout>